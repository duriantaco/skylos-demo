// web/src/utils/formatters.ts

export function formatDate(d: string): string {
  return String(d);
}

// UNUSED (demo): this function is never used
export function formatMoney(m: number): string {
  return `$${m}`;
}
